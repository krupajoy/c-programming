#include<stdio.h>
int main()
{
	int num,n,rem,sum=0;
	scanf("%d",&num);
	n=num;
	while(n!=0)
	{
		rem=n%10;
		sum=sum+rem*rem*rem;
		n=n/10;
	}
	if(sum==num)
	printf("%d is armstrong number",num);
	else
	printf("%d is not an armstrong number",num);
}
